{
  "name": "clipcc-extension-restart_project",
  "version": "1.0.1",
  "description": "greenFlag()",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack --bail",
    "build:dist": "NODE_ENV=production yarn run build"
  },
  "author": "NanHaiJuRuo",
  "license": "ISC",
  "dependencies": {
    "clipcc-extension-cli": "^2.2.1",
    "copy-webpack-plugin": "^11.0.0",
    "webpack-cli": "^4.10.0",
    "zip-webpack-plugin": "^4.0.1"
  },
  "devDependencies": {}
}
